//Display the longest word
#include<stdio.h>
#include<string.h>
int main(){
char words[5][20];
int i,maxlen=0;
printf("Enter 5 word:");
for(i=0;i<5;i++)
    scanf("%s",&words[i]);
    if (strlen(words[i]>maxlen))
}
maxlen=0;
strlen(words[i]);
index=i;
printf("longest word:%s\n",words[index]);
return 0;
}
